{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "High-load API Gateway with Fastify and http-proxy, cluster mode",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start:single": "node dist/single.js",
    "dev": "npm run build && node --watch dist/index.js",
    "build:watch": "tsc --watch"
  },
  "keywords": [
    "gateway",
    "fastify",
    "proxy",
    "cluster"
  ],
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@fastify/http-proxy": "^11.0.1",
    "@fastify/swagger": "^9.7.0",
    "@fastify/swagger-ui": "^5.2.5",
    "dotenv": "^16.3.1",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
